{?users}
	Users
	<ul>
		{#users auth=auth}
			{?auth}
				<li style="text-align:left"><a href="/users/{id}">{name}</a> :: {type}</li>
			{:else}
				<li style="text-align:left">{name} :: {type}</li>
			{/auth}
		{/users}

	</ul>
{:else}
	No users!
{/users}